<Window x:Class="RDPManager.TrayFlyout"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:RDPManager.Converters"
        mc:Ignorable="d"
        Title="RDP Manager Quick" Height="450" Width="320"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" Deactivated="Window_Deactivated">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:StatusToBrushConverter x:Key="StatusBrush"/>
    </Window.Resources>

    <Border Background="{StaticResource BgSidebarBrush}" CornerRadius="12" 
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Padding="16,12" Background="{StaticResource BgDeepBrush}" CornerRadius="11,11,0,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="18" Height="18" CornerRadius="4" Background="{StaticResource AccentBlueDarkBrush}" Margin="0,0,10,0">
                            <TextBlock Text="⊞" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="RDP Manager Pro" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Bold" FontSize="12"/>
                    </StackPanel>
                    <Button Content="✕" HorizontalAlignment="Right" Style="{StaticResource GhostButton}" 
                            Width="24" Height="24" Padding="0" Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <!-- Connection List -->
            <ScrollViewer Grid.Row="1" Margin="0,4" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding SortedConnections}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="itemBorder" Margin="8,4" Padding="16,12" CornerRadius="10" Background="Transparent" Cursor="Hand" MouseLeftButtonDown="Connection_Click">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Ellipse Grid.Column="0" Width="10" Height="10" Fill="{Binding IsOnline, Converter={StaticResource StatusBrush}}" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Host}" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    
                                    <Border Grid.Column="2" VerticalAlignment="Center">
                                        <TextBlock Text="▶" Foreground="{StaticResource AccentBlueBrush}" FontSize="14"/>
                                    </Border>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True" SourceName="itemBorder">
                                    <Setter TargetName="itemBorder" Property="Background" Value="{StaticResource BgHoverBrush}"/>
                                </Trigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Padding="12" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Ouvrir l'application complète" HorizontalAlignment="Stretch" Margin="0,0,8,0" Style="{StaticResource GhostButton}" Click="OpenMain_Click"/>
                    <Button Grid.Column="1" Content="Quitter" Style="{StaticResource GhostButton}" Foreground="#FF4b4b" Click="Quit_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
